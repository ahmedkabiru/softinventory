server:
  port: 8082

spring:
  application:
    name: category-service
  datasource:
    url: jdbc:postgresql://localhost:5432/softinventorydb
    username: kabiruahmed
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/